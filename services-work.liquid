---
layout: base.liquid
title: Our Services & Works
permalink: /services-works/
---

<div class="section container fade-in-section">
    <h2>Our Services & Works</h2>

    <div class="product-grid">
        
        <!-- 
          First, we combine the 'services' and 'works' collections into one list
          so they can appear in the same grid.
        -->
        {% assign combined_list = collections.services | concat: collections.works %}
        
        <!-- Now we loop through the combined list -->
        {% for item in combined_list %}
        <a href="{{ item.data.image }}" target="_blank" class="product-card">
            <div class="product-card-image-wrapper">
                <img src="{{ item.data.image }}" alt="{{ item.data.title }}">
            </div>
            
            <!-- 
              This block is copied DIRECTLY from products.liquid
              to ensure an identical look and feel.
            -->
            <div class="product-card-name" style="text-align: left; padding: 15px; font-weight: normal;">
                
                <!-- Title -->
                <div class="product-card-title" style="font-weight: bold; font-size: 1.1rem; text-transform: capitalize; color: #333;">
                    {{ item.data.title }}
                </div>
                
                <!-- Price -->
                {% if item.data.price %}
                <div class="product-card-price" style="font-weight: bold; font-size: 1.1rem; margin-top: 8px; color: #000;">
                    ₹{{ item.data.price }}
                </div>
                {% endif %}
                
                <!-- Category -->
                {% if item.data.category %}
                <div class="product-card-category" style="font-size: 0.9rem; color: #777; margin-top: 8px; text-transform: capitalize;">
                    {{ item.data.category }}
                </div>
                {% endif %}
                
                <!-- Description (Collapsible) -->
                {% if item.data.description %}
                <details style="margin-top: 10px; font-size: 0.9rem; color: #555; line-height: 1.4;">
                    <!-- This summary is the "Description" link -->
                    <summary style="cursor: pointer; font-weight: 600; color: #333; font-size: 0.9rem; text-transform: none;">
                        Description
                    </summary>
                    <!-- This is the content that shows/hides -->
                    <p style="margin-top: 5px; margin-bottom: 0; font-weight: normal;">
                        {{ item.data.description }}
                    </p>
                </details>
                {% endif %}

            </div>
        </a>
        {% endfor %}

        <!-- Update the 'if' condition to check the combined list -->
        {% if combined_list.length == 0 %}
            <p class='empty-gallery-message' style="position: static; transform: none; color: #333;">
                No services or works found. Please check back soon.
            </p>
        {% endif %}
    </div>
</div>
