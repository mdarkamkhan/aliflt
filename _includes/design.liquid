---
layout: base.liquid
---

<div class="container section">
  <div class="product-detail-grid">

    <div class="product-detail-image">
      {% if image %}
        <img src="{{ image }}" alt="{{ title | escape }}" style="width:100%; border:1px solid #eee;">
      {% else %}
        <img src="/icons/icon-512x512.png" alt="Design">
      {% endif %}
    </div>

    <div class="product-detail-info">
      <h1 class="product-detail-title">{{ title }}</h1>

      {% if price %}
        <p class="product-detail-price" style="font-size: 1.2rem; font-weight: bold;">
            Charges: ₹{{ price }}
        </p>
      {% endif %}

      {% if category %}
        <p class="product-detail-category">Use: {{ category }}</p>
      {% endif %}

      <div class="product-description" style="margin: 20px 0;">
        {{ content }}
      </div>

      <div class="product-detail-actions">
        {%- capture wapp_message -%}
        Hello, I am interested in this design: {{ title }}
        Link: https://aliflt.netlify.app{{ page.url }}
        {%- endcapture -%}

        <a href="https://wa.me/7250470009?text={{ wapp_message | strip | url_encode }}" 
           class="btn-buy" 
           target="_blank"
           style="width:100%; text-align:center;">
          Request This Design
        </a>
      </div>
    </div>

  </div>
</div>
