[build]
  publish = "."
  functions = "netlify/functions"

[[plugins]]
  package = "@netlify/plugin-copy-contents"
  [plugins.inputs]
    source = [
      "offers", 
      "products", 
      "service", 
      "works"
    ]
    destination = "netlify/functions"
