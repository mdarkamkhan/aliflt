---
layout: base.liquid
pagination:
  data: collections.products
  size: 1
  alias: product
  permalink: "products/{{ product.fileSlug }}/"
eleventyComputed:
  title: "{{ product.data.title }}"
---

<!-- 
==================================================
===== 💡 VISIBILITY FIX ==========================
================================================== 
REMOVED "fade-in-section" class to make this page
visible by default and fix the blank page bug.
-->
<div class="container section">
    
    <div class="product-detail-grid">
        
        <!-- Column 1: Image -->
        <div class="product-detail-image">
            <img src="{{ product.data.image | url }}" alt="{{ product.data.title }}">
        </div>
        
        <!-- Column 2: Info -->
        <div class="product-detail-info">
            <h1 class="product-detail-title">{{ product.data.title }}</h1>
            
            {% if product.data.price %}
                <span class="product-detail-price">₹{{ product.data.price }}</span>
            {% endif %}
            
            {% if product.data.category %}
                <span class="product-detail-category">{{ product.data.category }}</span>
            {% endif %}

            <!-- Add to Cart & Buy Now Buttons -->
            <div class="product-detail-actions">
                <button class="btn-cart" id="addToCartBtn" 
                        data-product-id="{{ product.fileSlug }}" 
                        data-title="{{ product.data.title | escape }}" 
                        data-price="{{ product.data.price }}" 
                        data-image="{{ product.data.image | url }}">
                    Add to Cart
                </button>
                <button class="btn-buy" id="buyNowBtn" 
                        data-title="{{ product.data.title | escape }}" 
                        data-price="{{ product.data.price }}" 
                        data-image="{{ product.data.image | url }}">
                    Buy Now
                </button>
            </div>
            
            <!-- 
            ==================================================
            ===== 💡 DESCRIPTION FIX =========================
            ================================================== 
            Changed 'product.templateContent' to 'product.data.description'
            to correctly show the description from the CMS.
            -->
            {% if product.data.description %}
                <div class="product-detail-description">
                    <h3>Description</h3>
                    <p>{{ product.data.description }}</p>
                </div>
            {% endif %}
        </div>
    </div>
</div>


