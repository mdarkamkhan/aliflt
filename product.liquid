---
layout: base.liquid
pagination:
  data: collections.products
  size: 1
  alias: product
  permalink: "products/{{ product.fileSlug }}/"
eleventyComputed:
  title: "{{ product.data.title }}"
---

<!-- 
==================================================
===== 💡 VISIBILITY FIX ==========================
================================================== 
REMOVED "fade-in-section" class to make this page
visible by default and fix the blank page bug.
-->
<div class="container section">
    
    <!-- 
    ==================================================
    ===== 💡 NEW DEBUG BOX ===========================
    ================================================== 
    This box is to find out why the page is blank.
    It will be bright yellow with a red border.
    -->
    <div style="position: fixed; top: 80px; left: 10px; right: 10px; z-index: 99999; background: #ffc; border: 3px solid #f00; color: #000; padding: 15px; font-size: 14px; font-family: monospace; line-height: 1.6;">
        
        <strong>--- DEBUGGER ACTIVE ---</strong><br>
        
        {% if product %}
            Page is rendering. Checking product data...<br>
            ------------------------------------<br>
            <strong>Title:</strong> {{ product.data.title | default: "MISSING" }}<br>
            <strong>Price:</strong> {{ product.data.price | default: "MISSING" }}<p>
            <strong>Image:</strong> {{ product.data.image | default: "MISSING" }}<br>
            <strong>Description:</strong> {{ product.data.description | default: "MISSING" }}<br>
            <strong>Slug:</strong> {{ product.fileSlug | default: "MISSING" }}<br>
            <strong>URL:</strong> {{ product.url | default: "MISSING" }}<br>
        {% else %}
            <strong>ERROR:</strong> The 'product' variable is empty or does not exist.
            This means the pagination is failing.
            Check 'collections.products' in .eleventy.js.
        {% endif %}
        
        <br>
        <strong>--- END DEBUG ---</strong>
    </div>

    
    <div class="product-detail-grid">
        
        <!-- Column 1: Image -->
        <div class="product-detail-image">
            <img src="{{ product.data.image | url }}" alt="{{ product.data.title }}">
        </div>
        
        <!-- Column 2: Info -->
        <div class="product-detail-info">
            <h1 class="product-detail-title">{{ product.data.title }}</h1>
            
            {% if product.data.price %}
                <span class="product-detail-price">₹{{ product.data.price }}</span>
            {% endif %}
            
            {% if product.data.category %}
                <span class="product-detail-category">{{ product.data.category }}</span>
            {% endif %}

            <!-- Add to Cart & Buy Now Buttons -->
            <div class="product-detail-actions">
                <button class="btn-cart" id="addToCartBtn" 
                        data-product-id="{{ product.fileSlug }}" 
                        data-title="{{ product.data.title | escape }}" 
                        data-price="{{ product.data.price }}" 
                        data-image="{{ product.data.image | url }}">
                    Add to Cart
                </button>
                <button class="btn-buy" id="buyNowBtn" 
                        data-title="{{ product.data.title | escape }}" 
                        data-price="{{ product.data.price }}" 
                        data-image="{{ product.data.image | url }}">
                    Buy Now
                </button>
            </div>
            
            {% if product.data.description %}
                <div class="product-detail-description">
                    <h3>Description</h3>
                    <p>{{ product.data.description }}</p>
                </div>
            {% endif %}
        </div>
    </div>
</div>
