---
layout: base.liquid
pagination:
  data: services  # 🎯 FIX 1: Point to 'services' (your data), not 'collections.services'
  size: 1
  alias: item 
  
permalink: false
  
# 🎯 FIX 2: Use the 'slug' filter for reliable URLs
pagination.permalink: "services/{{ item.title | slug }}/"

eleventyComputed:
  # 💡 Note: It's 'item.title' now, not 'item.data.title'
  # (because 'item' is the object from the data file directly)
  title: "{{ item.title }}" 
---

<h1 class="product-detail-title">{{ item.title }}</h1>
<span class="product-detail-price">₹{{ item.price }}</span>
<p class="accordion-description-text">{{ item.service_information }}</p>
